{#
/**
 * @file
 * Default theme implementation for a media layout.
 *
 * Available variables:
 * - content: The content for this layout.
 * - attributes: HTML attributes for the layout <div>.
 *
 * @ingroup themeable
 */
#}
{% if content %}
  {% if content.top %}
    <div {{ region_attributes.top.addClass('region--top') }}>
      {{ content.top }}
    </div>
  {% endif %}
  <div{{ attributes.addClass('row gx-8 gy-8') }}>
    <div{{ region_attributes.first.addClass('col-lg-3') }}>
      {{ content.first }}
    </div>
    <div{{ region_attributes.second.addClass('col-lg-6') }}>
      {{ content.second }}
    </div>
    <div{{ region_attributes.third.addClass('col-lg-3') }}>
      {{ content.third }}
    </div>
  </div>
  {% if content.bottom %}
    <div {{ region_attributes.bottom.addClass('region--bottom') }}>
      {{ content.bottom }}
    </div>
  {% endif %}
{% endif %}
